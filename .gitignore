# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts
.pnp.*

# Environment — NEVER commit real secrets (only .env.example is tracked)
.env
.env.*
!.env.example

# Test / coverage
coverage/
playwright-report/
test-results/
.nyc_output/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Vercel / provider caches
.vercel/
.turbo/

# Local scratch
*.local
tmp/

# Claude Code local (user-owned) settings — keep out of VCS
.claude/settings.local.json

# Playwright — reports + LOCAL-ONLY authenticated storageState (never commit)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
tests/.auth/

# Understand-Anything generated knowledge graph + cache (local tool artifact)
.ua/
.understand-anything/

.vercel
.env*

# Local AI skill tooling state (skill content + install lock) — not project config
.agent/
.agents/
skills-lock.json
